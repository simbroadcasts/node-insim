# node-insim

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build node-insim` to build the library.

## Running unit tests

Run `nx test node-insim` to execute the unit tests via [Jest](https://jestjs.io).
