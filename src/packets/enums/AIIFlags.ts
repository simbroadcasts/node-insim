export enum AIFlags {
  /** Detect if engine running */
  AIFLAGS_IGNITION = 1,

  /** Upshift lever currently held */
  AIFLAGS_CHUP = 4,

  /** Downshift lever currently held */
  AIFLAGS_CHDN = 8,
}
